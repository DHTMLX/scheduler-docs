---
sidebar_label: "recurring_overflow_instances"
title: "recurring_overflow_instances config"
description: "управляет обработкой повторений, выходящих за пределы текущего месяца"
---

# recurring_overflow_instances
:::warning
Это свойство работает только с устаревшим расширением для повторяющихся событий.
::: 
### Description

@short: Управляет обработкой повторений, выходящих за пределы текущего месяца

@signature: recurring_overflow_instances: string

### Example

~~~jsx
scheduler.config.recurring_overflow_instances = "lastDay";
~~~

**Default value:** undefined

### Details

:::note
 Для использования этого свойства необходимо активировать расширение [recurring](guides/extensions-list.md#recurring). 
:::

Рассмотрим событие, запланированное на 30-е число каждого месяца, и как оно ведет себя в феврале при различных настройках:

- **"skip"** - событие на несуществующую дату пропускается. *То есть, в феврале событие не произойдет.* 
- **"lastDay"** - событие на несуществующую дату переносится на последний день месяца. *В этом случае оно произойдет 28 февраля.*
- **"none"** - событие на несуществующую дату переносится на первый день следующего месяца. *Здесь событие состоится 1 марта.*

Если опция не задана, по умолчанию используется поведение "skip".

### Change log
- добавлено в версии v5.3.11
